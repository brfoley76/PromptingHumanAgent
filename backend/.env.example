# Agentic Learning Platform Configuration

# ============================================================
# AGENT CONFIGURATION
# ============================================================

# Agent type: 'simple' (rule-based) or 'llm' (AI-powered)
AGENT_TYPE=llm

# ============================================================
# LLM PROVIDER SETTINGS
# ============================================================

# LLM Provider: 'openai', 'anthropic', or 'none'
LLM_PROVIDER=openai

# OpenAI Configuration (if using OpenAI)
OPENAI_API_KEY=sk-your-api-key-here
MODEL_NAME=gpt-4-turbo-preview

# Anthropic Configuration (if using Claude instead)
# ANTHROPIC_API_KEY=sk-ant-your-api-key-here
# MODEL_NAME=claude-3-opus-20240229

# LLM Parameters
AGENT_TEMPERATURE=0.7
AGENT_MAX_TOKENS=500

# ============================================================
# DATABASE CONFIGURATION
# ============================================================

# Database URL (SQLite for development)
DATABASE_URL=sqlite:///./learning.db

# ============================================================
# SERVER CONFIGURATION
# ============================================================

HOST=0.0.0.0
PORT=8000
DEBUG=True

# ============================================================
# INSTRUCTIONS
# ============================================================

# To use the LLM agent:
# 1. Copy this file to .env
# 2. Add your OpenAI API key to OPENAI_API_KEY
# 3. Set AGENT_TYPE=llm
# 4. Run: python3 test_terminal.py

# To use the simple rule-based agent (no API key needed):
# 1. Set AGENT_TYPE=simple
# 2. Run: python3 test_terminal.py

# The system will automatically fall back to the simple agent
# if AGENT_TYPE=llm but no API key is configured.
